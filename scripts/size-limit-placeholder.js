// Referenced only by .size-limit.js as a fallback check when no real
// package has a size-limit.json yet — keeps `size-limit --json` from
// erroring on an empty config. Delete once the first package ships.
export default 0;
