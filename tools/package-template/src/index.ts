/**
 * Placeholder public API — replace with this package's real exports.
 * Only what's exported here is public; everything else belongs in
 * `src/internal/`.
 */
export function placeholder(): string {
  return 'PACKAGE_NAME';
}
