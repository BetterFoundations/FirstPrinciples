# EngineeringCompass Cursor skills

Project Agent Skills mirrored from `.claude/skills/` (Session 33 prompt library).

These `SKILL.md` files omit `disable-model-invocation`, so Cursor may auto-select them from description match. Adopters who clone EngineeringCompass into another workspace must copy `.cursor/skills/` (and/or `.claude/`) to **their workspace root** — see EngineeringCompass `README.md`.
