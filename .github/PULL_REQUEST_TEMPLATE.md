## Summary

What does this PR change, and why?

## Changeset

- [ ] I added a changeset (`pnpm changeset`) because this PR changes a
      published package (API, behaviour, types, or docs that ship with the
      package).
- [ ] This PR does **not** need a changeset (tooling, CI, governance, or
      other non-published paths only).

PRs that change package code without a changeset will fail the changeset
status check once that CI job is in place.

## Test plan

- [ ] I ran the relevant `pnpm` scripts locally (`lint` / `typecheck` / `test`
      / `build` as applicable).
- [ ] New or changed behaviour is covered by tests, or this PR is docs/tooling
      only.

## Checklist

- [ ] I have read [CONTRIBUTING.md](../CONTRIBUTING.md).
- [ ] This PR does not introduce secrets, credentials, or personal data.
- [ ] The description matches the actual diff (no unlabeled drive-by refactors).
