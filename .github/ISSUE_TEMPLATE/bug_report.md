---
name: Bug report
about: Report a reproducible bug in a FirstPrinciples package
title: '[bug] '
labels: bug
---

## Package

- **Package name** (e.g. `@firstprinciples/core`):
- **Package version** (npm version from `package.json` / the published tag — not a git SHA unless the bug is unreleased):

## What happened

A short description of the unexpected behaviour.

## Expected behaviour

What you expected instead.

## Reproduction

A bug report without a reproduction cannot be actioned. Include **one** of:

1. Minimal steps that fail against a published package, or
2. A small code snippet that compiles and demonstrates the issue, or
3. A link to a repository that reproduces it.

```ts
// Minimal reproduction
```

## Environment

- Node version (`node -v`):
- Package manager and version (`pnpm -v` / `npm -v`):
- OS:

## Additional context

Logs, error messages, or related issues.
